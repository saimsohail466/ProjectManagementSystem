<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to "Manage Projects", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <!-- checks if session is nil then display sign in and registration link -->
        <% if user_session.nil? %>
          <div class="flex">
            <div class="flex">
                <li class="nav-item">
                <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
              </li>
              <li>
                <%= link_to "Registration", new_user_registration_path, class: "nav-link" %>
              </li>
        <% else %>
            <% if current_user.admin? %>
                <%= link_to "Users", admin_users_path , class: "nav-link" %>
                <%= link_to "Projects", admin_projects_path , class: "nav-link" %>
            <% else %>
                <%= link_to "Users", users_path , class: "nav-link" %>
                <%= link_to "Projects", projects_path , class: "nav-link" %>
            <% end %>

            <% unless current_user.employee? %>
                <%= link_to "Clients", clients_path , class: "nav-link" %>
            <% end %>
            </div>
            <div class="flex"> 
              <li class="nav-item">
                <%= link_to "", edit_user_registration_path, class: "nav-link text-primary fas fa-user-edit", title: "Edit profile" %>
              </li>
               <li class="nav-item">
                <%= link_to "", destroy_user_session_path, method: :delete, class: "nav-link text-danger fas fa-power-off", title: "Logout" %>
              </li>
            </div>
        </div>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
