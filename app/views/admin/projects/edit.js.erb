$('.modal-backdrop').remove();
$('.project-edit-form').html("<%= j render 'form', project: @project %>");
$('#edit_project').addClass('show')
$('#edit_project').show()
$('body').addClass('modal-open')
